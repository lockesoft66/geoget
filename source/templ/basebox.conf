[sdl]
output=texture

[render]
aspect=off
glshader=none

[cpu]
cpu_cycles=max
cpu_cycles_protected=auto

[mouse]
mouse_capture=seamless

[autoexec]
@echo off
mount c {{TAG}} -t dir
c:
cd ensemble
loader
exit
